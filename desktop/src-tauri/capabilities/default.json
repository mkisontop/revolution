{
  "identifier": "default",
  "description": "Pet overlay + panel runtime capabilities",
  "windows": ["pet", "panel"],
  "permissions": [
    "core:default",
    "core:window:allow-start-dragging"
  ]
}
